package net.spross.app

/**
 * One home for the addresses the app publishes — the same rule the iOS side follows
 * (`Legal.contactAddress`): a mail printed in two places has to be one string.
 */
object Legal {
    const val CONTACT_ADDRESS = "spross@polygon.gmbh"
}
